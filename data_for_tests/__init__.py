"""
When we run tests, we need to have some login, some data, which we will send to tests, to DB.
Therefore, I suggest to keep such data as login in separate folder. I suggest to use @dataclass,
but it can be dictionary, simple classes, lists or even strings.
"""
